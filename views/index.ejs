<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div class="container">
        <h1>Todo List</h1>
        <% if(locals.list){ %>
            <% list.forEach(task=>{ %>
                <form action="/delete" method="post">
                    <input type="checkbox" name="checkbox" value="<%= task._id %>" onChange="this.form.submit()">
                    <p><%=task.listItem%></p>
                </form>
            <% });%>
        <% } %>

        <form  name="itemAdd" method="POST" action="/add">
            <input style="width: 80%;" class="cbox" type="text" name="wItem" >
            <input style="width: 15%;" class="cbox" type="submit" value="Add" >

        </form> 
    </div>
</body>
</html>